// postcss.config.mjs
const config = {
  plugins: {
    tailwindcss: {},       // Plugin principal de Tailwind
    autoprefixer: {},      // Añade prefijos automáticamente para compatibilidad con navegadores
  },
};

export default config;